#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")
#@ load("@ytt:struct", "struct")

#@ def customize():
#@overlay/match by=overlay.subset({"kind": "ServiceAccount", "metadata": {"name": "default"}}), expects="0+"
---
metadata:
  annotations:
    #@overlay/match missing_ok=True
    eks.amazonaws.com/role-arn: arn:aws:iam::739115551047:role/tap-workload
    #@overlay/match missing_ok=True
    datavalue: #@ str(struct.decode(data.values))
#@ end