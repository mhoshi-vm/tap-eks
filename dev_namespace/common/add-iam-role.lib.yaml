#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

---

#@ load("@ytt:overlay", "overlay")

#@ def customize():
  #@ if hasattr(data.values, "iamrole"):
#@overlay/match by=overlay.subset({"kind": "ServiceAccount", "metadata": {"name": "default"}}), expects="0+"
---
metadata:
  annotations:
    eks.amazonaws.com/role-arn: #@ "arn:aws:iam::" + data.values.iamrole + ":role/tap-workload"
  #@ end
#@ end